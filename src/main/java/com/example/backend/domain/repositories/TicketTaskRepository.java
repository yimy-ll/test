package com.example.backend.domain.repositories;/*
package com.psa.backend.domain.repositories;

import com.psa.backend.domain.entities.TicketTask;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.util.Collection;

@Repository
public interface TicketTaskRepository extends JpaRepository<TicketTask, Long> {

    Collection<TicketTask> findByTicketId(Long ticketId);

    Collection<TicketTask> findByTaskId(Long taskId);

    void deleteByTicketId(Long ticketId);

    void deleteByTaskId(Long taskId);
}

 */
